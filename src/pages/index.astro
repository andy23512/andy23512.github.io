---
import '@/styles/global.css';
import CelestialSphere from '../components/CelestialSphere.astro';
import StarfieldBackdrop from '../components/StarfieldBackdrop.astro';
import Layout from '../layouts/Layout.astro';
import { pages } from '../data/projects.ts';

// Controls both the display order of the filter pills and which badges show up as pills at all.
const badgeOrder = ['Practice', 'Statistics', 'Docs', 'Specs', 'Editor', 'app', 'extension'];

const sortedPages = pages.slice().sort((a, b) => a.title.localeCompare(b.title));
---

<Layout>
	<StarfieldBackdrop />
	<div class="text-white h-screen flex flex-col">
		<div class="relative px-4 py-2 pr-12 bg-gray-900/80 backdrop-blur-sm flex-none flex flex-col sm:flex-row sm:items-center sm:gap-2">
			<h1 class="text-2xl whitespace-nowrap">
				Tangent's Star Chart
			</h1>
			<span class="hidden sm:inline">-</span>
			<h2 class="text-lg">
				Unofficial CharaChorder web tools, apps, and extensions — a few named after stars
			</h2>
			<a
				href="/stars"
				class="self-start sm:ml-auto sm:self-auto shrink-0 rounded border border-blue-300/40 px-2 py-1 text-xs font-medium text-blue-200/80 transition hover:border-blue-300/70"
			>
				Naming stories
			</a>
			<button
				type="button"
				id="toggle-star-chart"
				aria-pressed="true"
				class="self-start sm:self-auto shrink-0 rounded border border-blue-300/40 px-2 py-1 text-xs font-medium text-blue-200/80 transition hover:border-blue-300/70"
			>
				Hide star chart
			</button>
			<a
				href="https://github.com/andy23512/andy23512.github.io"
				target="_blank"
				aria-label="View this site's source on GitHub"
				class="absolute top-2 right-3 rounded text-blue-200/40 transition hover:text-blue-100 focus-visible:text-blue-100 focus-visible:ring-2 focus-visible:ring-blue-300/70"
			>
				<svg viewBox="0 0 16 16" width="20" height="20" fill="currentColor" aria-hidden="true">
					<path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.01 8.01 0 0016 8c0-4.42-3.58-8-8-8z"/>
				</svg>
			</a>
		</div>
		<div class="flex-1 min-h-0 flex flex-col lg:flex-row">
			<div id="star-chart-wrapper" class="flex-none h-[34vh] lg:h-auto lg:w-2/5 xl:w-1/2">
				<CelestialSphere />
			</div>
			<div class="flex-1 min-h-0 overflow-auto">
				<div class="sticky top-0 z-10 bg-gray-900/90 backdrop-blur-sm p-4 pb-3 flex flex-col gap-2 border-b border-white/10">
					<label class="sr-only" for="project-search">Search projects</label>
					<input
						id="project-search"
						type="search"
						placeholder="Search projects…"
						class="w-full rounded bg-gray-700/60 px-3 py-1.5 text-sm placeholder:text-gray-400 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-300/70"
					/>
					<div class="flex flex-wrap gap-2">
						{
							badgeOrder.map(badge => (
								<button
									type="button"
									data-badge-filter={badge}
									aria-pressed="false"
									class="badge-filter rounded border border-blue-300/40 px-2 py-1 text-[11px] font-medium uppercase tracking-wide text-blue-200/80 transition hover:border-blue-300/70 aria-pressed:bg-blue-400/30 aria-pressed:text-blue-50 aria-pressed:border-blue-300/70"
								>
									{badge}
								</button>
							))
						}
					</div>
				</div>
				<div class="p-4">
					<div class="grid grid-cols-1 xl:grid-cols-2 gap-4">
						{
							sortedPages.map(page => {
								const header = (
									<h4 class="text-lg font-semibold mb-1">
										{page.title}
										{page.star && (
											<span
												class="ml-1 align-middle text-sm text-yellow-300/80"
												title="Named after a star — see Naming stories for why"
											>
												✦<span class="sr-only"> (named after a star, see Naming stories for why)</span>
											</span>
										)}
										{page.badges?.map(badge => (
											<span class="ml-2 align-middle rounded border border-blue-300/40 px-1.5 py-0.5 text-[11px] font-medium uppercase tracking-wide text-blue-200/80">
												{badge}
											</span>
										))}
									</h4>
								);
								const source = page.repo && (
									<a
										href={page.repo}
										target="_blank"
										aria-label={`${page.title} source on GitHub`}
										class="absolute top-3 right-3 rounded text-blue-200/40 transition hover:text-blue-100 focus-visible:text-blue-100 focus-visible:ring-2 focus-visible:ring-blue-300/70"
									>
										<svg viewBox="0 0 16 16" width="18" height="18" fill="currentColor" aria-hidden="true">
											<path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.01 8.01 0 0016 8c0-4.42-3.58-8-8-8z"/>
										</svg>
									</a>
								);
								return (
									<div
										data-star={page.star}
										data-badges={page.badges?.join(',') ?? ''}
										data-search={`${page.title} ${page.description}`.toLowerCase()}
										class="project-card rounded data-[star]:hover:ring-2 data-[star]:hover:ring-yellow-300/70 data-[star]:focus-within:ring-2 data-[star]:focus-within:ring-yellow-300/70"
									>
									{page.url ? (
										<a href={page.url} target="_blank" class="block p-4 pr-10">
											{header}
											<p class="text-sm">{page.description}</p>
										</a>
									) : (
										<div class="p-4 pr-10">
											{header}
											<p class="text-sm">{page.description}</p>
											<div class="mt-3 flex flex-wrap gap-2">
												{page.links.map(link => (
													<a
														href={link.url}
														target="_blank"
														class="inline-flex items-center rounded bg-gray-600/60 px-2.5 py-1 text-xs font-medium transition hover:bg-gray-500/70 focus-visible:ring-2 focus-visible:ring-blue-300/70"
													>
														{link.label}
													</a>
												))}
											</div>
										</div>
									)}
									{source}
									</div>
								);
							})
						}
					</div>
					<p id="no-results" class="hidden text-sm text-gray-400 mt-4">No projects match your filters.</p>
				</div>
			</div>
		</div>
	</div>
</Layout>

<style>
	.project-card {
		position: relative;
		/* Own stacking context so the ::before light can sit at z-index -1,
		   above the card background but below its content. */
		isolation: isolate;
		overflow: hidden;
		background-color: color-mix(in oklab, var(--color-gray-700) 70%, transparent);
		backdrop-filter: blur(4px);
		transition:
			background-color 0.3s,
			backdrop-filter 0.3s;
	}

	/* Pointing at a card clears it, opening a window onto the starfield behind
	   the page. focus-within covers keyboard focus landing on a link inside. */
	.project-card:hover,
	.project-card:focus-within {
		background-color: color-mix(in oklab, var(--color-gray-700) 10%, transparent);
		backdrop-filter: none;
	}

	/* A soft light trailing the cursor across the card being pointed at. */
	.project-card::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: -1;
		opacity: 0;
		transition: opacity 0.3s;
		background: radial-gradient(
			18rem circle at var(--pointer-x, 50%) var(--pointer-y, 50%),
			rgba(191, 219, 254, 0.22),
			transparent 70%
		);
	}

	.project-card:hover::before,
	.project-card:focus-within::before {
		opacity: 1;
	}
</style>

<script>
	for (const card of document.querySelectorAll<HTMLElement>('.project-card')) {
		card.addEventListener('pointermove', (event) => {
			const rect = card.getBoundingClientRect();
			card.style.setProperty('--pointer-x', `${event.clientX - rect.left}px`);
			card.style.setProperty('--pointer-y', `${event.clientY - rect.top}px`);
		});
	}
</script>

<script>
	const searchInput = document.querySelector<HTMLInputElement>('#project-search');
	const badgeButtons = document.querySelectorAll<HTMLButtonElement>('.badge-filter');
	const cards = document.querySelectorAll<HTMLElement>('.project-card');
	const noResults = document.querySelector<HTMLElement>('#no-results');
	const activeBadges = new Set<string>();

	function applyFilters() {
		const query = (searchInput?.value ?? '').trim().toLowerCase();
		let visibleCount = 0;
		for (const card of cards) {
			const badges = (card.dataset.badges ?? '').split(',').filter(Boolean);
			const matchesBadge = activeBadges.size === 0 || badges.some(badge => activeBadges.has(badge));
			const matchesQuery = query === '' || (card.dataset.search ?? '').includes(query);
			const visible = matchesBadge && matchesQuery;
			card.hidden = !visible;
			if (visible) visibleCount++;
		}
		noResults?.classList.toggle('hidden', visibleCount > 0);
	}

	searchInput?.addEventListener('input', applyFilters);

	for (const button of badgeButtons) {
		button.addEventListener('click', () => {
			const badge = button.dataset.badgeFilter;
			if (!badge) return;
			if (activeBadges.has(badge)) {
				activeBadges.delete(badge);
				button.setAttribute('aria-pressed', 'false');
			} else {
				activeBadges.add(badge);
				button.setAttribute('aria-pressed', 'true');
			}
			applyFilters();
		});
	}
</script>

<script>
	const starChartWrapper = document.querySelector<HTMLElement>('#star-chart-wrapper');
	const toggleButton = document.querySelector<HTMLButtonElement>('#toggle-star-chart');
	const STORAGE_KEY = 'starChartHidden';

	function setHidden(hidden: boolean) {
		if (!starChartWrapper || !toggleButton) return;
		starChartWrapper.hidden = hidden;
		toggleButton.setAttribute('aria-pressed', String(!hidden));
		toggleButton.textContent = hidden ? 'Show star chart' : 'Hide star chart';
	}

	setHidden(localStorage.getItem(STORAGE_KEY) === 'true');

	toggleButton?.addEventListener('click', () => {
		if (!starChartWrapper) return;
		const hidden = !starChartWrapper.hidden;
		setHidden(hidden);
		localStorage.setItem(STORAGE_KEY, String(hidden));
	});
</script>
